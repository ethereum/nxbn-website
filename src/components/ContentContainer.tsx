import React from "react"
import { Container } from "@chakra-ui/react"

const ContentContainer = (props) => {
  return <Container maxW="1536px" {...props} margin="0 auto"/>
}

export default ContentContainer
